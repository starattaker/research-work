# Python
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Download on target machine (Zenodo / Ultralytics / pip)
data/DenPAR/
data/*.zip
*.pt
!artifacts/local_viz/*.pt

# Clone separately if needed
official_repo/

# Training outputs — retrain or resume locally
runs/

# Bundled weights for local viz (friend pushes, you pull) — see scripts/push_local_viz_to_github.sh
!artifacts/
!artifacts/local_viz/
!artifacts/local_viz/**

# Local IDE (skills are under .cursor/skills/ — included if present)
.cursor/projects/
.cursor/*.json

# Papers (keep locally)
*.pdf

# OS
.DS_Store
Thumbs.db
desktop.ini
