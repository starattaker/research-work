# Python
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Download on target machine (Zenodo / Ultralytics / pip)
data/DenPAR/
data/*.zip
*.pt

# Clone separately if needed
official_repo/

# Training outputs — retrain or resume locally
runs/
.cursor/projects/
.cursor/*.json

# Papers (keep locally); reference PDFs at repo root, built paper output, etc.
*.pdf
# ...but figure PDFs under paper/figures/ are source assets the thesis
# \includegraphics's directly - they must be tracked, not treated as a
# downloaded paper. (Caught the TikZ diagram PDFs silently for two commits
# before this was noticed - see commit history.)
!paper/figures/**/*.pdf

# OS
.DS_Store
Thumbs.db
desktop.ini
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore

# LaTeX build artifacts
*.aux
*.log
*.out
*.synctex.gz
